{
  "name": "@campsite/editor",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf .turbo dist node_modules",
    "lint": "eslint ./src/*.ts*",
    "test": "vitest --environment=jsdom"
  },
  "dependencies": {
    "@campsite/regex": "workspace:*"
  },
  "devDependencies": {
    "@campsite/eslint-config": "workspace:*",
    "@campsite/tsconfig": "workspace:*"
  }
}